#include "stdafx.h"

/*
void Stroke::apply(string name, string value) {
    if (name == "stroke") {
        color = apply(value);
    }
    else if (name == "stroke-width") {
        width = stof(value);
    }
    else if (name == "stroke-opacity") {
        opacity = stof(value);
    }
}
*/